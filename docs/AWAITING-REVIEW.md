# 사장님 확인/작업 대기 목록

> 자리 비우신 동안 자동으로 진행하고, **결정/확인이 필요한 것만** 여기 모아둡니다.
> 전부 검증·커밋·푸시되어 **Vercel 자동배포**됩니다(라이브: https://kor-study-rihee.vercel.app).

## 🆕🆕🆕🆕 4대 축 완성 배치 — 마스터리·실사공주·개인화·시간제한 (모두 라이브 확인됨)

요청하신 4개 방향(학습 깊이·과목 균형·흥미/개인화·부모/안전)을 **모두 채웠습니다.** 라이브 번들에서 문자열까지 확인했습니다.

- ⭐ **레슨 마스터리 별(정답률 기반)** — 한 판의 오답 수로 별 1~3개(오답0=별3). 보상 화면에 ⭐⭐⭐ 표시, 과목 화면 트랙 헤더에 누적 별 합계.
- ✨ **‘진짜 공주’ 새 메뉴(실사풍)** — 기존 공주 꾸미기는 그대로 두고, **AI 생성 실사풍 공주 룩 5종**(장미·겨울·라벤더·정원·루비)을 별로 해금. 큰 미리보기 + 2열 룩 선택.
- 👨‍👩‍👧 **우리 가족 읽기(개인화)** — 보호자 화면에서 **아이 이름·가족 단어(엄마·아빠 등, 최대 12개)** 를 넣으면, 아이가 큰 카드를 눌러 통글자로 읽음(개인화 = 4세 동기부여 최상위). 커스텀 단어라 브라우저 음성.
- ⏰ **하루 놀이 시간 제한(스크린타임)** — 보호자가 정한 분(없음/10/15/20/30/45)이 지나면 **‘오늘은 여기까지 🌙’ 쉬는 시간 잠금**. 화면 보일 때만 시간 누적. 잠금 화면에서 **보호자 곱셈 확인 후 10분 연장** 가능. 보호자 화면은 안 잠금.
- 🧸 **마스코트 ‘콩콩이’ + 화면별 동화풍 배경** — 홈 인사, 보상 화면 등에 등장. 한글/숫자/영어/홈마다 다른 AI 생성 배경 일러스트로 ‘없어 보임’ 해소.
- 🔢 **숫자 발음 재생성** — 들어갈 때 어색하던 숫자(하나~열) 음성 다시 생성.
- 🖼 **이미지 생성 파이프라인(비용0)** — 제미나이 ‘웹’을 에이전트가 직접 제어해 배경·마스코트·실사공주를 런타임 비용 없이 번들 자산으로 생성(API 미사용).

**품질**: `npm run build`(tsc -b)·eslint 0·테스트 **137개** 통과. 부모 게이트는 공용 컴포넌트로 정리(리포트+잠금 공유).

**배포 상태(중요)**: 한때 Vercel 승급 큐가 지연됐었으나 **스스로 풀렸고**, 라이브 sw.js = 로컬 빌드와 바이트 일치(`index-U5gx1gLu.js`). 위 기능 문자열(‘오늘은 여기까지’·‘우리 가족’·‘하루 놀이 시간’·‘진짜 공주’·‘콩콩이’) 전부 라이브 번들에 포함 확인.

> 확인 부탁: ①첫 한글 진입 시 인트로 음성(로컬 목소리로 나는지) ②실사 공주 그림이 마음에 드는지(원하면 골드·화이트 룩 추가 생성) ③시간 제한 기본은 **‘제한 없음’** — 켜둘 분을 정해 주세요.

---

## 🆕🆕🆕 상용앱화 배치 — 과목 허브 + 영어 + 보상/복습/부모 (모두 라이브)

- 🏠 **홈을 과목 선택 허브로 재구성**: 첫 화면에서 **📖한글 · 🔢숫자 · 🔤영어**를 고르면 그 과목만 보임(쭉 나열 문제 해결). 놀이방·보호자 분리.
- 🔤 **영어(ABC) 과목 신설**: 알파벳 A~Z + 예시단어, 게임 3종(오늘의 알파벳·듣고 찾기·따라쓰기). 발음은 브라우저 영어 음성.
- 📚 **낱말 도감**(배운 단어 수집) · 🏆 **뱃지·업적**(12종) — 모으는 재미.
- 🔁 **복습(SRS)**: 틀린 단어를 모아 다시 듣고 찾기(맞히면 졸업). 4세 안전상 난이도 상향 대신 복습으로.
- 📊 **부모 리포트 강화**: 최근 7일 막대그래프 + 하루 목표(±설정). 홈 ‘오늘의 목표’가 목표/오늘횟수로 표시.
- 📱 **태블릿 대응**: 모바일 레이아웃을 비율 그대로 확대해 큰 화면을 채움(폰 무변화). ✍️ **따라쓰기**(쓰기) 글자 트랙에 추가.

**품질**: `npm run build`(tsc -b)·eslint 0·테스트 **114개** 통과, 라이브 일치. (이전 배포 실패는 `tsc -b` 미검증 탓 → 이제 `npm run build`로 검증)

**다음 후보(미구현)**: 레슨별 마스터리 별(정답률), 학습시간 제한 강제, 영어 따라쓰기 음가 정교화, 짧은 문장 읽기.

---

## 🆕🆕 종합 학습앱 배치 — 새 과목·부모기능·받침 (모두 라이브)

“자모 분리만으론 상용앱 대비 부족하다”는 피드백에 따라, 상용앱 **전체 기능 인벤토리**를 다시 조사(코드 재사용성까지 검증)해 비용0로 큰 폭 추가했습니다.

- 🔢 **수개념 트랙 신설(새 과목!)** — 홈에 ‘숫자 배우기’ 추가. 게임 3종: 오늘의 숫자, **세어 보기**(이모지를 세어 숫자 고르기, 하나·둘·셋 읽어 줌), **많고 적음 비교**. 1~10, 이미지 자산 0(이모지).
- 📝 **받침 글자 트랙** — ‘글자 만들기’에 **받침(종성) 3번째 슬롯** 추가(자음+모음+받침=강·산·밤·물). 자모 트랙이 받침까지 이어져 진짜 한글 읽기에 한 발 더.
- 📊 **부모 리포트 + 보호자 게이트** — 홈 하단 ‘보호자’ 입구. **곱셈 게이트**(아이 차단, 오답마다 새 문제)로 보호 후, 배운 글자/숫자/단어 놀이 수·연속출석·모은 별·스티커·배운 단어 목록을 한눈에. 서버 없이 로컬 집계.
- 신규 숫자 수사·받침 음절·안내 **부모 목소리(여성) TTS** 프리캐시 완료.

**품질**: tsc·eslint 0, 테스트 **111개** 통과, 빌드 OK. 적대적 리뷰(수·받침·부모 4차원→검증) 반영 — 부모 게이트 무차별 대입 차단, 학습단어 손상값 방어 등.

### 추가 완료(라이브)
- ✍️ **따라쓰기(쓰기)** — 글자 윤곽을 손가락으로 따라 칠하면 덮은 비율로 관대하게 완성. **모든 글자 레슨에 추가**(보고→만들고→찾고→써보기, 학습지식 흐름).
- 📱 **태블릿 화면 대응** — 모바일 레이아웃을 비율 그대로 확대해 큰 화면을 채움(폰은 무변화). ※ 태블릿에서 그리기/따라쓰기 좌표가 정상인지 한 번 확인 부탁(스펙상 정상이나 실기기 확인 권장).

### 남은 로드맵(우선순위 — 다음에 이어서)
1. **간격 반복 복습(라이트 SRS)** — 틀린 글자/단어 재출제.
2. **낱말카드 도감** — 배운 단어 수집 갤러리.
3. **별 보상 상점** — 별 소비처(꾸미기 해금형 전환).
4. **뱃지·업적**, **통문자 플래시 읽기**(그림 없이).

> ⚠️ 로컬 TTS 서버가 꺼져 있을 때 생성한 ‘따라쓰기 안내’ 음성 1개는 브라우저 음성으로 나옵니다. 서버 켜고 `node scripts/gen-tts.mjs` 한 번 돌리면 부모 목소리로 교체됩니다.

---

## 🆕 상품성 강화 배치 — 시중 학습지 조사 → ‘한글 깨치기’ 트랙 신설 (모두 라이브)

**조사 요약(구몬·눈높이·토도한글·EBS·Khan Academy Kids 등 + 교육학):**
시중 학습지/앱은 공통적으로 **자음·모음 → 글자 조합(가갸거겨) → 받침 → 단어 → 문장**의 ‘한글 깨치기’ 척추를 갖습니다.
우리 앱은 이 단계를 건너뛰고 바로 통글자 맞히기로 점프해 체계성·상품성이 약했습니다(과학적 읽기·2015 교육과정도 경고하는 구조).
→ **단일 최대 격차 = ‘자모 트랙’ 부재.** 마침 `decompose.ts`에 글자 조합 함수가 이미 있어 **비용0·신규 자산 0**으로 메울 수 있었습니다.

**이번에 구현(자모 트랙):**
- 📖 **글자 배우기 트랙 신설**(홈 상단). 4세 맞춤 게이팅 — 명료한 모음(ㅏㅗㅣ/ㅓㅜㅡ) → 쉬운 자음+모음 결합(가고기·나노니·마모미·사소시·아오이). 받침 조작은 뒤로 미룸.
- **글자 게임 3종**: ①오늘의 글자(소리 듣기) ②**글자 만들기**(자음+모음=글자, 한글 조합 원리 체험) ③듣고 같은 글자 찾기(글자-소리 대응).
- 통글자(단어) 트랙은 그대로 두고 **그 위에 자모 층을 얹는 균형 설계**(통글자를 버리지 않음 — 4세 음소 인식 발달 근거).
- 글자 음가/안내 **부모 목소리(여성) TTS 프리캐시** 완료.
- 🔥 **연속 출석(스트릭) + 오늘의 목표**: 리텐션 레버. 단 4세 배려로 **하루 빠짐은 프리즈로 인정**, ‘깨짐=처벌’ 문구 없음.

**품질**: tsc·eslint 0, 테스트 **105개** 통과, 빌드 OK. 한글 조합 로직·스트릭 TDD 검증.

### 조사 기반 다음 로드맵(우선순위 — 다음에 이어서 구현 가능)
1. **간격 반복 복습 큐(라이트 SRS)** — 틀린 글자/단어를 다음 세션 앞에 재출제(난이도 상향 대신 ‘복습 빈도’로, 4세 안전).
2. **부모 게이트 + 주간 리포트(로컬 대시보드)** — 곱셈 게이트로 보호, 무엇을 배웠는지 주간 집계. 서버 없이 비용0.
3. **마스터리 게이트 + 적응형** — 라운드 정답률로 별3·해금/재출제.
4. **이름·우리 가족 통글자 + 수집 도감** — 개인화 동기.
5. **획순 따라쓰기**(점선 트레이싱) — 임팩트 높으나 4세 소근육 고려해 ‘따라긋기’까지. 그림판 캔버스 재사용 가능.

---

## 이번 배치 (요청하신 피드백 반영, 모두 라이브)

- **이야기 재미있게**: 6편을 의성어·서사 흐름이 있는 **동화체**로 재작성(주인공 아기 곰 ‘콩콩이’). 배울 단어는 그대로 강조.
- **자동 진행**: 이야기 **‘다음’ 버튼 제거**, 낭독이 끝나면 **3초 뒤 자동으로 다음 장면**(4세 터치 부담↓). 안전 타이머 병행, 진행 점 표시. (어른은 그림을 탭하면 바로 넘어감)
- **문제 보기 똑똑하게**: 그림 퀴즈는 **같은 종류의 비슷한 그림**을, 단어 퀴즈는 **같은 글자수** 보기를 우선 → 생뚱맞은 보기 사라지고 너무 쉽지 않게.
- **공주 얼굴/아이템 크게**: 머리·얼굴을 키운 **치비 비율**로 왕관·액세서리가 또렷하게 보임. (모든 왕관/헤어/배경 시각검증)
- **드레스 화려하게**: 단색 → **무늬(도트·별·하트·반짝, 드레스마다 다름) + 허리 리본 새시 + 밑단 레이스 프릴**.
- **배경음악 추가**: 부드러운 자장가풍 멜로디 루프. 홈 좌상단 **🔊/🔇 토글**로 끄고 켤 수 있음(설정 저장).
- **정답·보상 소리**: 문제를 맞히면 **밝은 칭찬 효과음**, 보상 화면에선 **팡파르 + 방금 모은 스티커** 표시.
- **새 보상 2종**:
  - 🏅 **스티커 책** — 놀이를 끝낼 때마다 스티커를 한 장씩 모아 26칸 책을 채움(누르면 이름 말해줌).
  - 🎨 **그림 그리기** — 손가락 색칠/그리기. **자유 그리기 + 색칠 도안 5종(사과·별·나비·집·꽃)**, 색 팔레트·붓 크기·지우개·전체 지우기.

> 효과음·배경음악은 **오디오 파일 없이** 브라우저에서 합성합니다(추가 비용 0·오프라인 동작). 휴대폰에서 **첫 터치 후** 소리가 납니다(브라우저 자동재생 정책).

## 지금까지 누적 완료
- 커리큘럼 **26 레슨(14주)**·단어 **72개**, 꾸미기 아이템 **79종**, 로컬 TTS 부모 목소리(동화/새 문구 포함 재생성).
- 폰 터치 개선, 게임 안내 음성, 꾸미기 구매/뽑기 확인 팝업·두구두구 연출·별 차감 애니메이션.

**품질**: tsc·eslint 0, 테스트 **96개** 통과, 빌드 OK(프리캐시 7.7MB). 공주는 resvg PNG로 시각검증.
이번 배치는 **다중 에이전트 적대적 리뷰**(7개 차원 → 발견 항목 회의적 검증)를 추가로 돌려, 확정된 결함을 반영했습니다:
이야기 자동진행 경쟁 조건(그림 탭 시 옛 타이머가 잘못된 장면으로 진행) 차단, 그림판 회전/리사이즈 좌표 어긋남(ResizeObserver) 보정,
배경음악 노드 누수 정리, 손상 저장의 잘못된 스티커 id 방어, 왕관 트윈클 기준점 보정 등.

---

## ✅ 확인/결정 (가벼운 것)

1. **배경음악**: 기본 **켜짐**(아주 작은 볼륨). 거슬리면 홈 🔊 버튼으로 끄면 됩니다 — 기본을 꺼짐으로 바꿀까요?
2. **그림판**: 흰 도화지에 그리고 **지우개는 흰색으로 지웁니다**(도화지 색으로 복원). 도안 버튼을 누르면 새 도화지로 바뀝니다(색칠북 방식). 나가면 그림은 사라집니다 — 저장/갤러리가 필요하면 추가할게요.
3. **user-scalable=no**: 터치 안정을 위해 화면 확대(핀치줌)를 막았습니다. 확대가 필요하면 되돌릴 수 있어요.
4. 태블릿/폰에서 ①이야기 자동진행 ②문제 난이도 ③스티커·그림 ④배경음악/효과음을 한 번 봐주세요.

## 다음에 더 할 수 있는 것
- 색칠 도안·스티커 종류 더 늘리기 / 그림 저장·자랑하기
- 콘텐츠 더(숫자·반대말 등) / 동화 삽화(연습레슨에 그림 추가)
- 부모 목소리 녹음으로 일부 멘트 교체(`public/audio/`에 넣고 매핑) — 로컬 TTS 위에 우선 적용됨.

> 새 그림은 `node scripts/render-princess.mjs`(PNG 렌더)로 눈으로 확인하며 만듭니다.
> 음성은 `node scripts/gen-tts.mjs`로 (멱등) 재생성합니다.
