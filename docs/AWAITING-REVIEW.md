# 사장님 확인/작업 대기 목록

> 제가 자리 비운 동안 자동으로 못 하는 것(계정 로그인·AI 아트 생성 등)만 여기 모아둡니다.
> 돌아오셔서 위에서부터 처리하시면 됩니다. 나머지는 제가 다 구현·검증·푸시해 둡니다.

## 자리 비운 동안 완료한 것 (전부 검증·커밋·푸시됨)
- 이야기 장면 일러스트(과일나라 6장) — 손으로 만든 SVG 그림책
- **놀이 3종 추가**: 글자 만들기·글자 찾기·짝맞추기 (이제 놀이 5종)
- **동화 2편 추가**: 🐶동물 친구들, 🌙반짝반짝 밤하늘 (이제 동화 3편·장면 18장)
- 홈에서 동화 선택(목록), Vercel 배포 설정, lint 0 problems, 52 테스트 통과

---

## 1) Vercel 배포 연결 (한 번만, 약 3분)

코드/설정은 모두 준비됨([vercel.json](../vercel.json) 포함). 아래만 해주시면 바로 라이브됩니다.

1. https://vercel.com 접속 → **Continue with GitHub** 로 로그인(계정 없으면 자동 가입).
2. **Add New… → Project** 클릭.
3. `heemkim7/kor-study` 저장소 **Import**.
   - (처음이면 "Install Vercel for GitHub" 로 저장소 접근 권한 한 번 허용 — `kor-study`만 선택해도 됨.)
4. 설정 화면은 **그대로 두고**(자동 감지: Framework=Vite, Build=`npm run build`, Output=`dist`) → **Deploy**.
5. 1~2분 뒤 `https://kor-study-xxxx.vercel.app` 주소가 나옵니다. → 태블릿에서 그 주소로 접속!
   - 홈 화면에서 "홈 화면에 추가"(공유 메뉴) 하면 앱처럼 전체화면 + 오프라인 동작.

> 이후에는 제가 `git push` 할 때마다 Vercel이 **자동 재배포**합니다(추가 작업 없음).
> 주소를 더 예쁘게 바꾸고 싶으면 Vercel 프로젝트 → Settings → Domains 에서 변경 가능.

---

## 2) 다음 단계 — 방향만 정해주시면 제가 진행합니다

지금까지(동화 3편·놀이 5종)는 제가 더 만들 수 있는 안전한 범위였고, 다음 큰 한 걸음은
**보상→공주 꾸미기(드레스업)**인데 여기서 사장님 손이 필요합니다.

- **A. 공주 꾸미기(프린세스메이커) — 가장 기대하셨던 기능**
  - 제가 할 것: 스티커북 + 옷·헤어·악세사리 레이어 합성/상점 시스템 구현
  - **사장님이 할 것: 공주 아트를 브라우저 로그인 AI로 생성**(API 비용 0).
    프롬프트 팩 준비됨 → `docs/assets/princess-image-prompts.md`
- **B. 동화 더 추가** (예: 🚌탈것, 👪가족 등) — 제가 전부 자동 진행 가능
- **C. 스티커북만 먼저** (모은 스티커 구경) — 제가 자동 진행 가능

> 참고: 지금 개발 미리보기의 스크린샷/렌더가 환경 문제로 불안정해서, 새 그림은
> `node scripts/render-scenes.mjs`(PNG 렌더)로 제가 직접 눈으로 확인하며 만들고 있어요.
> React 화면 레이아웃은 보수적으로만 바꿨습니다(태블릿에서 한 번 봐주시면 좋아요).
